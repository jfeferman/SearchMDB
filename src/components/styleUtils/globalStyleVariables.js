export const colors = {
  white: '#ffffff',
  black: '#000000',
  blue: '#176DDC',
  hoverBlue: '#0B356C',
  lightGray: '#DDDDDD',
};

/**
 * Transform pixel to rem values
 * @param {number} px The pixel value to be transformed
 */
export const px2rem = (px) => `${px / 16}rem`;
