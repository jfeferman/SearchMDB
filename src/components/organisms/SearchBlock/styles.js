import styled from 'styled-components';
import { px2rem } from '../../styleUtils/globalStyleVariables';

export const StyledSearchBlock = styled.div`
  margin: ${px2rem(100)} auto ${px2rem(100)} auto;
  
  h1 {
    text-align: center;
  }
`;
